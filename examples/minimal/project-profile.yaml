project: fictional-maintenance-service
context:
  max_words: 350
gates:
  specification: requires approved decisions and traceability
  implementation: requires approved contracts and security review
  release: requires executed evidence and release approval
